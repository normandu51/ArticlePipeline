.lavish
node_modules
dist
*/.DS_Store
.DS_Store

# Persistent Chrome profile with NYT login cookies (runtime data)
.chrome_profile/

# Kokoro TTS model — runtime dependency, downloaded separately (see KOKORO_SETUP.md)
tts/Kokoro/Kokoro-82M/
tts/Output/

# SQLite databases / project DB — runtime data, kept on disk only
articles.db
articles.db-shm
articles.db-wal
articles.sqbpro